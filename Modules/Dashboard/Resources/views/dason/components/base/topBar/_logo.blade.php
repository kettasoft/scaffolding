<div class="d-flex align-items-center me-5">
    <a href="{{ route('home') }}" class="logo logo-dark">
                    {{-- <span class="logo-sm">
                        <img src="{{ app_logo() }}" alt="{{ app_name() }}" height="30">
                    </span> --}}
        <span class="logo-lg">
                        <img src="{{ app_logo() }}" alt="{{ app_name() }}" height="50">
                        {{-- <span class="logo-txt">{{ app_name() }}</span> --}}
                    </span>
    </a>

    {{-- <a href="{{ route('home') }}" class="logo logo-light">
                    <span class="logo-sm">
                        <img src="{{ app_logo() }}" alt="{{ app_name() }}" height="30">
                    </span>
        <span class="logo-lg">
                        <img src="{{ app_logo() }}" alt="{{ app_name() }}" height="24"> <span
                class="logo-txt">{{ app_name() }}</span>
                    </span>
    </a> --}}
</div>
